{
  "name": "diagrams-for-agents-local-plugin",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "examples": "node scripts/build-examples.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run examples && npm test && node scripts/verify-package.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
